
    // const texte = document.querySelector(".texte2");
    // const mainAnimation = document.querySelector(".animation");

    // // Fonction pour ajuster la translation et l'opacité en fonction du défilement
    // const handleScroll = () => {
    //     const scrollTop = window.scrollY || document.documentElement.scrollTop;
    //     const animationTop = mainAnimation.offsetTop; // Position supérieure de la section animation
    //     const animationHeight = mainAnimation.offsetHeight; // Hauteur de la section animation
    //     const windowHeight = window.innerHeight; // Hauteur de la fenêtre

    //     // Vérifie si l'utilisateur a atteint la section animation
    //     if (scrollTop + windowHeight >= animationTop && scrollTop < animationTop + animationHeight) {
    //         // Déclenche l'animation une fois la section visible
    //         texte.style.transform = "translateX(0)"; // Translate le texte vers le centre
    //         texte.style.opacity = 1; // Augmente l'opacité
    //     } else {
    //         // Remet les valeurs initiales si l'utilisateur sort de la section
    //         texte.style.transform = "translateX(-100%)"; // Changer à -100%
    //         texte.style.opacity = 0;
    //     }
    // };
    // window.addEventListener("scroll", handleScroll);